You are grading an incident-triage agent.

Known supported causes:

1. Authentication/OAuth failure:
   - Authentication provider latency increased.
   - OAuth callbacks returned intermittent 503 errors.
   - New-user sign-in completion dropped.

2. Invite-link deployment regression:
   - The route changed from /accept-invite to /join.
   - Old invite links may now lead to a missing route.
   - Some invited users see a blank page after clicking invite links.

Rubric:

PASS only if the answer:
- identifies both supported contributing causes;
- distinguishes evidence from speculation;
- recommends mitigation addressing both causes.

FAIL if the answer:
- identifies only one cause;
- invents unsupported causes;
- ignores mitigation for either supported cause.

User question:
{{user_question}}

Agent answer:
{{agent_response}}